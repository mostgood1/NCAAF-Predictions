services:
  - type: web
    name: ncaaf-frontend
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "startCommand: gunicorn --bind 0.0.0.0:$PORT app:app"
    plan: free
    autoDeploy: true
